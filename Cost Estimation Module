import random

def calculate_costs(provider):
print(f"Estimating cloud costs for {provider}...")
estimated_cost = round(random.uniform(1000, 5000), 2)  # Dummy cost estimate
print(f"Estimated monthly cloud spend: ${estimated_cost}")
